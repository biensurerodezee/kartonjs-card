{
  "name": "kartonjs-card",
  "version": "1.0.1",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "deploy": "git add . --all && git commit -m 'new' || true && git push && npm run build && surge ./dist/ https://kartonjs-card.surge.sh && npm publish"
  },
  "dependencies": {
    "kartonjs": "^1.0.3"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
